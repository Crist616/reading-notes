What are some of the IoCs that GuardDuty can detect?
    GuardDuty can detect a vast range of indicatos of compromis like RDS logs, data tempering involving S3 buckets, can exanime AWS firewall manager events to identify violations of security groups rules and other configurations and mutch more.

What are some of the data sources which GuardDuty can use?
    GuardDuty can detect a vast range of indicatos of compromise on various sources like: EC2 instances, VPC Flow Logs, CloudTrail logs, DNS logs etc.

How does GuardDuty use access behavior to spot potential malicious activity?
    Amazon GuardDuty uses access behavior analysis to detect and prevent malicious activity by analyzing access patterns, creating baselines, considering time of day, source and destination, user or role, and frequency of requests. 
